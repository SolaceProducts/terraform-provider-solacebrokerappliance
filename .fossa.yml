version: 3

project:
  name: terraform-provider-solacebrokerappliance
  teams: []
  labels:
    - go-dependencies
    - terraform-provider
    - EBP-Appliance

vendoredDependencies:
  forceRescans: false
  scanMethod: CLILicenseScan
  licenseScanPathFilters:
    only:
      - "**/LICENSE"
    exclude:
      - "./.git"
      - "./.github"

paths:
  exclude:
    - ./.git
    - ./.github

telemetry:
  scope: full
